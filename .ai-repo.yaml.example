# AI Repository Configuration Example
#
# This file shows the available configuration options for aimgr.
# Copy this file to ~/.config/aimgr/aimgr.yaml and customize as needed.

# install.targets: Specifies which AI coding tools to install to by default
# Valid values: claude, opencode, copilot
# Default: [] (empty, will use existing directories)
#
# - claude: Claude Code (supports commands, skills, and agents)
# - opencode: OpenCode (supports commands, skills, and agents)
# - copilot: GitHub Copilot (supports skills only, no commands or agents)

# Example: Single target
install:
  targets:
    - claude

# Example: Multiple targets
# install:
#   targets:
#     - claude
#     - opencode

# Example: All supported tools
# install:
#   targets:
#     - claude
#     - opencode
#     - copilot
